export function Pitch() {
  return (
    <section className="pitch">
      <div className="wrap">
        <div className="row">
          <span className="eyebrow muted">// the case</span>
          <div>
            <p>
              Vector is the best log, metric, and trace pipeline engine.
              Managing it across a fleet by hand is not.
            </p>
            <p>
              VectorFlow is the missing control plane. A visual editor for the
              canvas, a fleet view for the rollout, and operational guard rails
              — version history, environments, alerts, anomaly detection — for
              the days when something drifts. Self-hosted. AGPL-3.0. Your data
              never leaves your network.
            </p>
          </div>
        </div>
      </div>
    </section>
  );
}
