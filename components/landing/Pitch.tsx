export function Pitch() {
  return (
    <section className="mx-auto max-w-3xl px-6 py-16 text-center">
      <p className="text-lg leading-relaxed text-fd-muted-foreground">
        Vector is the best log/metric/trace pipeline engine — but managing it
        across a fleet by hand is painful. VectorFlow gives platform engineers
        a visual editor, a fleet view, and the operational guard rails Vector
        is missing. Self-hosted, AGPL-3.0, no vendor lock-in.
      </p>
    </section>
  );
}
